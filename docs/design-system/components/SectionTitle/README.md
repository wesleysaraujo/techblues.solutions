Cabeçalho de seção: um rótulo curto (`eyebrow`, Inter Light, cor `azul-techblues`) seguido do título (`titulo-h2`, Inter Black, cor `cinza-escuro-techblues`). Três variantes de alinhamento/fundo: esquerda sobre branco, centro sobre branco, e centro sobre `azul-techblues` (usa `eyebrow`/`titulo-h2` em `branco` neste caso). Na fonte, o mesmo título aparece em uso real a 40px em vez dos 36px do componente — trate 36px como o padrão e 40px como variação observada, não como um segundo token.
