Rótulo de categoria/tag, pílula pequena em `rotulo-tag` (Open Sans ExtraBold, 10px, caixa alta) sobre fundo `azul-techblues` com texto `branco`, raio `raio-tag`. Na fonte, os dois estados "ativa" e "inativa" do componente estavam salvos com a mesma cor — trate a variante inativa como um alvo de definição futura (ex.: mesma pílula com fundo `cinza-claro-techblues` e texto `cinza-escuro-techblues`) em vez de reaplicar a cor ativa.
