Cartão para a seção "áreas de atuação", com título (`titulo-h4`) e um parágrafo curto (`texto-normal`). Raio `raio-md`, borda fina de 0.5px em `azul-techblues` em todas as variantes de fundo.

Variantes de fundo: padrão (um azul bem claro, quase branco, com texto `cinza-escuro-techblues`), `azul-techblues` sólido com texto `branco`, e `laranja` sólido com texto `cinza-max`. Existe também uma variante vertical (mesma composição, proporção mais alta) para grades de 3+ colunas em telas estreitas.
