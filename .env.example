# Endpoint que recebe os formulários (contato, Zoho, newsletter) via POST.
# Por padrão usa o proxy /api/lead (configurado no vercel.json e vite)
# ou a URL direta do webhook n8n (ex.: https://automacoes.integratudo.com.br/webhook/techblues-lead).
# Sem essa variável, o formulário de contato abre o WhatsApp com a mensagem preenchida.
PUBLIC_LEAD_ENDPOINT=/api/lead

# Token estático do usuário de integração no Directus (AgentOS). NÃO é usado pelo site:
# serve para a credencial "Directus AgentOS" do n8n e para docs/n8n/verificar-permissoes.mjs.
# O valor real fica só no .env (fora do Git).
DIRECTUS_KEY=

# Chatbot de diagnóstico (Typebot). Com o ID preenchido, o bot substitui o formulário.
# PUBLIC_TYPEBOT_ID é o "public id" do bot publicado (ex.: tech-blues-diagnostico).
# PUBLIC_TYPEBOT_API_HOST só é necessário se o Typebot for self-hosted (ex.: https://bot.techblues.com.br).
PUBLIC_TYPEBOT_ID=
PUBLIC_TYPEBOT_API_HOST=
